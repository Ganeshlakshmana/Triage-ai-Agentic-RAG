# ─────────────────────────────────────────────
# requirements.txt — Project Dependencies
# ─────────────────────────────────────────────
# Install all dependencies with:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────


# ── LLM Providers ─────────────────────────────
anthropic>=0.25.0
# Anthropic Claude SDK — primary LLM provider.

openai>=1.30.0
# OpenAI SDK — secondary LLM provider (gpt-4o-mini).
# Used in multi-provider routing fallback chain.

google-genai>=0.8.0
# Google Gemini SDK — tertiary LLM provider (gemini-2.0-flash).
# Used in multi-provider routing fallback chain.


# ── LangChain / LangGraph ─────────────────────
langgraph>=0.2.0
# LangGraph StateGraph for structured agentic pipelines.
# Replaces hand-written loop with a typed state machine.

langchain>=0.2.0
# LangChain core — prompt templates, message types, runnable protocol.

langchain-anthropic>=0.2.0
# LangChain adapter for Anthropic Claude.

langchain-openai>=0.1.0
# LangChain adapter for OpenAI.

langchain-google-genai>=1.0.0
# LangChain adapter for Google Gemini.


# ── Embeddings ────────────────────────────────
sentence-transformers>=2.7.0
# Local embedding model (all-MiniLM-L6-v2).
# Runs entirely on-device — no API calls, no rate limits.


# ── API & Server ──────────────────────────────
fastapi
uvicorn
python-multipart


# ── Vector Database ───────────────────────────
qdrant-client>=1.9.0
# Official Python client for Qdrant.
# Lets us create collections, insert vectors, and search.


# ── Environment & Config ──────────────────────
python-dotenv>=1.0.0


# ── Data Handling ─────────────────────────────
pandas>=2.0.0
tqdm>=4.66.0
tiktoken>=0.7.0


# ── Async Support ─────────────────────────────
asyncio-throttle>=1.0.2